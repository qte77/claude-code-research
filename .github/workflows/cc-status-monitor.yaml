# Polls Anthropic Statuspage API for Claude platform incidents and upserts
# into outages.jsonl archive. Opens a PR when new/updated incidents are found.
# Output: triage/status-monitor/outages.jsonl + outage-stats.md
name: Status Monitor

on:
  schedule:
    - cron: '0 9 * * 1'  # Weekly Monday 09:00 UTC
  workflow_dispatch:
  repository_dispatch:
    types: [status-change]  # Retained for future webhook proxy integration

permissions:
  contents: write
  pull-requests: write

jobs:
  collect:
    name: Collect Claude platform incidents
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97  # v7.0.0
        with:
          python-version: '3.12'

      - name: Save webhook payload
        if: github.event_name == 'repository_dispatch'
        env:
          WEBHOOK_PAYLOAD: ${{ toJSON(github.event.client_payload) }}
        run: echo "$WEBHOOK_PAYLOAD" > /tmp/webhook-payload.json

      - name: Collect incidents
        id: collect
        run: |
          set +e
          ARGS=(--archive triage/status-monitor/outages.jsonl)
          if [ -f /tmp/webhook-payload.json ]; then
            ARGS+=(--webhook-payload /tmp/webhook-payload.json)
          fi
          python .github/scripts/status-collector.py "${ARGS[@]}"
          echo "changed=$( [ $? -eq 1 ] && echo true || echo false )" >> "$GITHUB_OUTPUT"

      - name: Generate statistics
        if: steps.collect.outputs.changed == 'true'
        run: |
          python .github/scripts/status-stats.py \
            --archive triage/status-monitor/outages.jsonl \
            > triage/status-monitor/outage-stats.md

      - name: Create triage PR
        if: steps.collect.outputs.changed == 'true'
        uses: ./.github/actions/create-triage-pr
        with:
          branch-prefix: chore/outage-archive
          extra-files: triage/status-monitor/outages.jsonl triage/status-monitor/outage-stats.md
          skip-report: 'true'
          pr-title-prefix: 'chore: outage archive update'
          pr-body-header: 'Claude Platform Status Update'
          pr-body-note: 'Auto-generated by status monitor. Updates outages.jsonl and outage-stats.md.'
