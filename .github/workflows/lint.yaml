# Runs markdownlint-cli2 and lychee via existing Makefile targets.
# TODO: extract into a standalone reusable action (gha-md-lychee) — see issue.
name: Lint

on:
  push:
    branches: [main]
    paths:
      - '**/*.md'
      - '.markdownlint.json'
      - 'lychee.toml'
      - 'Makefile'
      - '.github/workflows/**'
      - '.github/actions/**'
      - '.github/scripts/**'
      - 'scripts/**'
      - 'tests/**'
  pull_request:
    branches: [main]
    paths:
      - '**/*.md'
      - '.markdownlint.json'
      - 'lychee.toml'
      - 'Makefile'
      - '.github/workflows/**'
      - '.github/actions/**'
      - '.github/scripts/**'
      - 'scripts/**'
      - 'tests/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  markdown:
    name: Markdown lint (markdownlint-cli2)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Set up Node
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020  # v7.0.0
        with:
          node-version: '20'

      - name: Install markdownlint-cli2
        run: make setup_mdlint

      - name: Run markdownlint
        run: make check_docs

  links:
    name: Link check (lychee)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Cache lychee binary
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9  # v6.1.0
        with:
          path: ~/.local/bin/lychee
          # Bump suffix (v1 -> v2) to force a fresh lychee download
          key: lychee-${{ runner.os }}-v1

      - name: Install lychee
        run: make setup_lychee

      - name: Run lychee
        run: make check_links

  actions:
    name: Action lint (actionlint)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Install actionlint
        run: make setup_actionlint

      - name: Run actionlint
        run: make check_actions

  status:
    name: Doc status tokens (check_status)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Validate doc frontmatter status tokens
        run: make check_status

  tests:
    name: Unit tests (unittest)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Run unit tests
        run: make test
